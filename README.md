#
# How to setup
#

```
cd ~
git clone git@github.com:naoto43/dot-files.git .dot-files
ln -s  .dot-files/dot-zshrc .zshrc
ln -s  .dot-files/dot-screenrc .screenrc
ln -s .dot-files/dot-gitconfig .gitconfig
ln -s .dot-files/dot-emacs.d .emacs.d
```
